[project]
name = "dl-sandbox"
version = "0.1.0"
description = "Deep learning sandbox: neural nets, autograd, and classifiers built from scratch with NumPy"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "colorama>=0.4.6",
    "graphviz>=0.21",
    "matplotlib>=3.10.8",
    "numpy>=2.4.2",
    "seaborn>=0.13.2",
    "torch>=2.10.0",
]
